@model DetailsViewModel

@{
    ViewData["Title"] = "Details Item";
}

@*<div class="container">
    <div class="row ">
    <img src="@Model.ImageUrl" class="img-fluid d-block w-100" alt="Responsive image">
    <div class="">
    <h4 class="text-center">@Model.Name - @Model.Price $</h4>
    <div class="row gx-5">
    <div class="col">
    <div class="p-3 border bg-light">@Model.Description</div>
    </div>
    </div>
    <ul class="list-group list-group-flush m-5" >
    Ingridients:
    @foreach (var item in @Model.Ingridients)
    {
    <li class="list-group-item col-md-3">@item.Name</li>
    }
    </ul>
    </div>
    </div>
    </div>*@

<div class="container bootdey">
    <div class="col-md-12">
        <section class="panel">
            <div class="panel-body">
                <div class="col-md-6">
                    <div class="pro-img-details">
                        <img src="@Model.ImageUrl" alt="">
                    </div>

                </div>
                <div class="col-md-6">
                    <h4 class="pro-d-title">
                        <a href="#" class="">
                            @Model.Name
                        </a>
                    </h4>
                    <p>
                        @Model.Description
                    </p>
                    <div class="product_meta">
                        @*<span class="posted_in"> <strong>Categories:</strong> <a rel="tag" href="#"></a> <a rel="tag" href="#"></a> <a rel="tag" href="#"></a> <a rel="tag" href="#"></a></span>*@
                        <span class="tagged_as">
                            <strong class="Ingridiens">Ingridients:</strong>
                            @foreach (var item in @Model.Ingridients)
                            {
                                <span>@item.Name</span>
                            }
                        </span>
                    </div>
                    <div class="m-bot15"> <strong>Price : </strong> <span class="price">@Model.Price $</span>  @*<span class="pro-price"> $300.00</span>*@</div>
                    <div class="form-group">
                        <label>Quantity</label>
                        <input type="quantiy" placeholder="1" class="form-control quantity">
                    </div>
                    <p>
                        <button class="btn btn-round btn-danger" type="button"><i class="fa fa-shopping-cart"></i> Add to Cart</button>
                    </p>
                </div>
            </div>
        </section>
    </div>
</div>